doctype html
html(lang="vi")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="stylesheet", href="/public/css/home.css")
    title Đăng ký — MusicApp
  body
    .modal-overlay
      .modal(role="dialog" aria-modal="true")
        .modal-close
          a(href="/") ✕
        .modal-inner
          .modal-heading
            h3 Đăng ký
          form.register-form(action="/register", method="post")
            .input-wrap
              input#username(type="text" name="username" placeholder="Tên hiển thị" required)
            .input-wrap
              input#email(type="email" name="email" placeholder="Email của bạn" required)
            .input-wrap.has-eye
              input#password(type="password" name="password" placeholder="Tạo mật khẩu" required)
              button.eye-toggle(type="button", aria-label="Hiện/ẩn mật khẩu") 👁️
            .input-wrap.has-eye
              input#confirm(type="password" name="confirm" placeholder="Nhập lại mật khẩu" required)
              button.eye-toggle(type="button", aria-label="Hiện/ẩn mật khẩu") 👁️
            .consent
              label
                input(type="checkbox" name="agree")
                span Tôi đã đọc và đồng ý các điều khoản
            button.modal-btn(type="submit") Tạo tài khoản
          .switch-auth
            p Đã có tài khoản? #[a(href="/login") Đăng nhập]
