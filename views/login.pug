doctype html
html(lang="vi")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="stylesheet", href="/public/css/home.css")
    title Đăng nhập — MusicApp
  body
    .modal-overlay
      .modal(role="dialog" aria-modal="true")
        .modal-close
          a(href="/") ✕
        .modal-inner
          .modal-heading
            h3 Đăng nhập
          form.login-form(action="/login", method="post")
            .input-wrap
              input#email(type="text" name="username" placeholder="Nhập email/username của bạn" required)
            .input-wrap.has-eye
              input#password(type="password" name="password" placeholder="Nhập mật khẩu của bạn" required)
              button.eye-toggle(type="button", aria-label="Hiện/ẩn mật khẩu") 👁️
            .row-between
              label.checkbox-label
                input(type="checkbox" name="remember")
                span Nhớ cho lần đăng nhập tới
              a.forgot(href="#") Quên mật khẩu?
            .consent
              label
                input(type="checkbox" name="agree")
                span Tôi đã đọc, hiểu rõ và tự nguyện đồng ý các điều khoản
            button.modal-btn(type="submit") Đăng nhập
          .switch-auth
            p Chưa có tài khoản? #[a(href="/register") Đăng ký]
