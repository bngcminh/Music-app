doctype html
html(lang="vi")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Quản lý playlist - Admin MusicApp
    link(rel="stylesheet", href="/public/css/admin.css")
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css")
  body
    .admin-wrapper
      aside.admin-sidebar
        .admin-brand
          .brand-icon
            i.fas.fa-music
          .brand-text
            h1 MusicApp Admin
            p Quản trị hệ thống

        nav.admin-nav
          a.nav-tab(href="/admin")
            i.fas.fa-chart-line
            span Bảng điều khiển
          a.nav-tab(href="/admin/users")
            i.fas.fa-users
            span Người dùng
          a.nav-tab(href="/admin/artists")
            i.fas.fa-microphone
            span Nghệ sĩ
          a.nav-tab(href="/admin/songs")
            i.fas.fa-music
            span Bài hát
          a.nav-tab.active(href="/admin/playlists")
            i.fas.fa-list-ul
            span Playlist

        .sidebar-footer
          a.btn-back(href="/")
            i.fas.fa-arrow-left
            span Về trang chủ

      main.admin-main
        header.admin-header
          .header-left
            h1#page-title Quản lý playlist
            p#page-desc Tạo và quản lý playlist

          .header-right
            .user-info
              .avatar
                i.fas.fa-user-circle
              .user-detail
                if user
                  p.user-name #{user.username}

        #notification.notification

        section.panel.active(data-panel="playlists")
          .panel-header
            h2
              i.fas.fa-list-ul
              span Quản lý playlist

          .form-card
            h3 Tạo playlist mới
            form.admin-form#playlist-create-form(action="/admin/create/playlist" method="post" enctype="multipart/form-data")
              .form-row
                .form-group
                  label
                    i.fas.fa-heading
                    span Tên playlist
                  input.form-control(type="text" name="playlistName" placeholder="Nhập tên playlist" required)

                .form-group
                  label
                    i.fas.fa-link
                    span Ảnh playlist
                  input.form-control(type="file" name="coverUrl" accept="image/*")

                .form-group
                  label
                    i.fas.fa-link
                    span Thêm bài hát
                  select.form-control(name="songs" multiple size="6")
                    each song in songs
                      option(value=song._id) #{song.songName}

              button.btn.btn-primary(type="submit")
                i.fas.fa-plus
                span Tạo playlist

          .panel-header.mt-4
            h3 Danh sách playlist

          .data-table-container
            table.data-table
              thead
                tr
                  th #
                  th Tên playlist
                  th Cover
                  th Số bài hát
                  th Thao tác
              tbody#playlists-body
                each playlist, index in playlists
                  tr
                    td #{index + 1}
                    td #{playlist.playlistName}
                    td
                      img(src=playlist.coverUrl alt="Cover" width="80")
                    td #{playlist.songs.length}
                    td
                      .action-buttons
                        form(action=`/admin/delete/playlist/${playlist._id}` method="GET")
                          button.btn.btn-danger.btn-sm.admin-action-btn(type="submit")
                            i.fas.fa-trash
                            span Xóa
                        a.btn.btn-secondary.btn-sm.admin-action-btn(href=`/admin/playlist/${playlist._id}`)
                          i.fas.fa-pen
                          span Sửa
